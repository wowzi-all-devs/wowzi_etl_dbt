
version: 2

models:
  - name: intercom_stg__conversations
    description: "First campaign date by influencer"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null