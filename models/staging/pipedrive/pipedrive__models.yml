
version: 2

models:
  - name: pipedrive_stg__organizations
    description: "Organization from Pipedrive"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null